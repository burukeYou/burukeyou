server:
  port: 9030


spring:
  application:
    name: search-server

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848



custom:
  ouath2:
    client:
      signingKey: 909090
      resourceId: ${spring.application.name}
      ignoreUrls:
        - /oauth/**
        - /actuator/**
  swagger2:
    basePackage: burukeyou.search.controller


